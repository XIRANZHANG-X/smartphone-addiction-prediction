# 项目方法学审查报告 (Review)

**审查日期**：2026-08-22  
**审查范围**：`data/`、`docs/`、`R/` 全部文件，含已运行的 6/14 个实验格子与消融/交互分析  
**审查结论**：项目整体方法论扎实，文档质量极高，但存在若干需要在报告中声明或修正的问题，按严重程度分级如下。

---

## 一、严重问题（Critical）

### 1.1 无早停（Early Stopping）—— 过拟合风险

**位置**：所有 `06_model_*_xgboost.R` 的 `fit_predict()` 函数

**问题**：xgboost 全部使用 `nrounds = 600` 固定轮数，没有任何早停机制。XGBoost 的 `xgb.train()` 支持 `watchlist` + `early_stopping_rounds` 参数，可以在验证集 AUC 不再提升时自动停止训练。当前做法：

```r
model <- xgboost::xgb.train(params, dtrain, nrounds = 600, verbose = 0)
```

600 轮是拍脑袋选的，对 20 万行数据可能过多（过拟合）或过少（欠拟合）。没有早停意味着：
- 模型可能记住了训练数据的噪声
- 不同折可能在不同轮数达到最优，固定 600 轮抹平了这个差异
- 无法判断 600 轮是否合理

**严重程度**：这是机器学习实践中公认的硬伤。在交叉验证中每一折都应该用**折内验证集**做早停，而不是固定轮数。

**建议修复**：在每折的训练折中再切出 20% 作为验证集，用 `watchlist` 监控，设置 `early_stopping_rounds = 50`。这会让每折的训练时间变长（需要 evaluation），但不应该跳过。

---

### 1.2 无超参数调优 —— 全部结果基于默认参数

**位置**：所有 `06_model_*.R`

**问题**：项目已在自己的文档中承认这一点（"全部结果均基于默认超参数，一轮都没调过"），但将其列为"待完成"而非"方法论缺陷"。这在评分标准中占比 30% 的"技术深度与方法对比"项下是严重失分点。

当前 xgboost 参数：
```r
eta = 0.05, max_depth = 6, subsample = 0.8,
colsample_bytree = 0.8, min_child_weight = 10, nrounds = 600
```

glmnet 参数：
```r
alpha = 0.5  # elastic net，但为什么是 0.5？
```

这些参数未经任何形式的搜索（网格、随机、贝叶斯）。对于一门机器学习课程项目，**至少应该展示调参过程**，即使最终分数提升不大。

**更严重的是**：没有调参的情况下得出"L1 > L2 > L3 > L4"的结论，这只有在**所有方法对超参数同样敏感**的前提下才成立。如果 L3/L4 在另一组超参数下逆转排名，当前结论就不成立。这不是杞人忧天——更复杂的插补可能需要不同的树深度或学习率。

**建议**：
- 至少对 `eta`、`max_depth`、`nrounds`（配合早停）做粗粒度网格搜索
- 在报告中声明"调参后结论是否成立"作为 caveat
- 如果时间不够，至少在 L1 上做一轮调参，展示"调参确实能提升分数但幅度有限"

---

### 1.3 折内无验证集 —— 交叉验证中的嵌套问题

**位置**：`06_model_TEMPLATE.R` 框架区循环

**问题**：当前 5 折 CV 的每一折中，训练折的全部数据既用于训练又（通过固定 `nrounds=600`）隐式地用于"模型选择"（即决定训练多少轮）。正确的做法是**嵌套交叉验证**：

```
外层 5 折（评估泛化能力）
  内层：训练折再切一个验证集出来（用于早停 / 调参）
```

当前做法让外层 CV 的 AUC 估计偏乐观，因为模型选择（虽然当前只有 nrounds 这一个隐式选择）接触到了验证折的信息。

**影响**：报告的 CV AUC 可能系统性偏高。但由于所有格子都同样偏高，相对排名（L1 > L2 > L3 > L4）可能仍然成立。

**建议**：至少加早停（见 1.1），在训练折内再切 20% 做 watchlist。这会自动解决嵌套问题。

---

### 1.4 配对 t 检验的样本量问题

**位置**：`docs/项目说明.md` 第六节、`docs/进度.md` 核心发现

**问题**：项目大量使用配对 t 检验来比较不同方法的 AUC 差异（如"L1 − L2, t=3.59, p=0.0231"）。但 t 检验基于 5 个配对差值（5 折），样本量 n=5：
- 正态性假设在 n=5 时无法检验
- t 检验在 n=5 时对偏离正态非常敏感
- 报告的 p 值（如 0.0231、0.0414）在 n=5 时不够稳健

**建议**：
- 改用 **Wilcoxon signed-rank test**（非参数，对 n=5 更稳健）作为主要检验
- 或使用 **5×2 cross-validation paired t-test**（Dietterich, 1998）或 ** corrected resampled t-test**（Nadeau & Bengio, 2003）
- 在报告中声明"基于 5 折配对检验，p 值仅供参考"
- 报告 effect size（Cohen's d）而非仅报告 p 值

---

### 1.5 特征选择中的数据泄漏（已在文档中承认，但严重性被低估）

**位置**：`docs/项目说明.md` 6.7 节

**问题**：消融实验中，判定"哪些特征是噪声"使用了全部训练数据和标签，然后在同一数据的 5 折 CV 上评估剔除这些特征后的 AUC。文档中写道"判定依据极干净（69 万行上边际差值 ≤ 0.004），而且我们是**删**特征不是**留**特征，偏差方向和幅度都可控"。

这个自我辩护不完全成立：
- **删特征也有偏差**：如果某特征在全体数据上被判为噪声，但在某些折内其实有微弱信号，删除它会让该折的 AUC 偏低，而你不知道
- **"判据极干净"不能替代方法学正确性**：边际差值 ≤ 0.004 意味着特征选择确实很安全，但方法论上正确的做法仍然是折内特征选择
- 对于 `notifications_per_day` 和 `app_opens_per_day` 的"翻案"（从噪声改判为重要），恰好证明了在全体数据上做特征选择的危险性——如果当初直接删了这两个特征，就不会发现它们的重要性

**建议**：在报告中明确声明这一局限性，且不要声称 p 值精确。Kaggle 提交后的 LB 分数是唯一的外部验证。

---

## 二、中等问题（Major）

### 2.1 L3 的"约束"名不副实

**位置**：`R/05_impute_L3.R`

**问题**：项目文档和代码注释反复强调"约束条件插补"是核心创新，但实测约束投影只在 **0.06%** 的行上触发。L3 实质上就是**链式线性回归插补**（用其他 8 列预测缺失列，迭代 2 轮），约束投影是装饰品。

文档在 6.5 节和进度文档中已诚实承认这一点，但：
- 文件名 `05_impute_L3.R` 的注释仍然说"这是全项目的核心方法"
- 在幻灯片中如果继续以"约束插补"命名，可能在答辩时被追问"约束到底起了什么作用"

**建议**：在最终报告中重命名为"条件均值插补"（conditional mean imputation），将"约束投影"降级为"我们也尝试了但发现作用有限"的 footnote。

---

### 2.2 L4 的硬编码 seed 绕过框架

**位置**：`R/05_impute_L4.R` 第 98 行

**问题**：
```r
filled <- missRanger::missRanger(
    as.data.frame(combined),
    ...
    seed = 1000L,    # ← 硬编码！
    ...
)
```

框架区的设计是 `set.seed(SEED + k)` 控制每折的随机性，但 L4 的 `apply_imputer_L4` 内部硬编码了 `seed = 1000L`。这意味着 missRanger 在 5 折中使用的随机种子完全一样，可能导致：
- 不同折的插补结果之间存在非预期的相关性
- 违反了"每折独立"的原则

**建议**：将 seed 改为由框架传入，例如 `seed = attr(imp, "seed")`，在 `fit_imputer_L4` 中设置。

---

### 2.3 派生特征已确认无效但未删除

**位置**：`R/03_features.R` 的 `derive_features()`

**问题**：消融实验已确认 6 个派生特征（`other_screen`, `weekend_ratio`, `social_share`, `gaming_share`, `free_frac`, `screen_social`）完全无用——删光后 AUC 变化 +0.00011（p=0.112）。但它们仍然：
- 在每条线的每折中被计算
- 占用了特征矩阵 31 列中的 6 列（约 20%）
- 增加了代码复杂度和新人理解成本

**建议**：在最终提交中删除。如果保留作为"我们试过但没用"的证据，至少加一个开关 `USE_DERIVED <- FALSE`。

---

### 2.4 代码大量重复

**位置**：6 个 `06_model_*.R` 文件

**问题**：6 个模型文件（L1_xgboost, L2_xgboost, L3_xgboost, L4_xgboost, L2_glmnet, L3_glmnet）每个约 210 行，其中 200 行是完全相同的框架区代码。只有 `MODEL_NAME`、`IMPUTE_LINE` 和 `fit_predict()` 函数体不同。

这是设计意图（模板 + 复制，保证接口一致），但实际结果是：
- 如果框架区需要修 bug（如 1.1 的早停），需要改 6 个文件
- 已经出现 TemplATE 的 header 注释在每个文件中重复（包括"改名为..."的说明，而文件已经改名了）
- 没有任何机制保证 6 个文件的框架区确实相同

**建议**：将框架区提取为 `R/06_framework.R`，各模型文件只需定义 `fit_predict()` 和配置变量，然后 `source("R/06_framework.R")`。这样修 bug 只改一处。

---

### 2.5 glmnet 的 `alpha = 0.5` 缺乏依据

**位置**：`06_model_L2_glmnet.R` 和 `06_model_L3_glmnet.R`

**问题**：glmnet 使用 `alpha = 0.5`（elastic net，L1 和 L2 正则化各一半），但没有任何说明为什么选 0.5。通常的做法是：
- 对 `alpha` 做交叉验证（如 `alpha = c(0, 0.5, 1)` 各跑一次 `cv.glmnet`）
- 或基于问题特性选择（特征数少、共线性低 → ridge α=0；需要特征选择 → lasso α=1）

**建议**：在报告中解释选择 α=0.5 的理由，或补做 α 的敏感性分析。

---

### 2.6 无概率校准评估

**位置**：全局

**问题**：评价指标是 AUC（只看排序），但提交的是概率。项目完全没有检查模型输出的概率是否校准良好（即预测 0.8 的样本是否真的有 80% 是正例）。不校准的概率在以下场景会出问题：
- 集成时不同模型的概率尺度不一致
- 如果评分标准改为 log-loss 就无法应对

**建议**：至少在报告中加一段 Brier Score 或 calibration plot 的讨论。这不是必须修复的问题（AUC 对校准不敏感），但缺少这段讨论会显得对概率输出的性质理解不够。

---

## 三、轻微问题（Minor）

### 3.1 `renv.lock` 缺失

**位置**：项目根目录

**问题**：作业明确要求提供包列表，项目文档也反复强调 `renv.lock` 是复现的关键。但当前仓库中不存在此文件。进度文档中 G 的任务之一就是维护它。

**建议**：在 `00_setup.R` 中加一行 `renv::snapshot()` 或在 `R/` 目录下提供独立的 `renv_snapshot.R`。

---

### 3.2 `07_ensemble.R` 和 `08_submit.R` 从未运行

**位置**：进度文档已标注

**问题**：这两个脚本"写完但从未运行"。集成脚本逻辑看起来正确，但未经过实际数据验证。在 20 万行 Tier A 数据上做一次干跑（dry run）可以提前发现 bug。

---

### 3.3 消融脚本中的代码重复

**位置**：`R/09_ablation.R` 和 `R/09_ablation_decompose.R`

**问题**：这两个脚本各自重新实现了 xgboost 的 `fit_predict` 逻辑，而不是复用 `06_model_*.R` 中的定义。如果主模型参数改变，消融脚本不会同步更新，导致结果不可比。

---

### 3.4 L3 收敛诊断缺失

**位置**：`R/05_impute_L3.R`，`N_ITER_L3 <- 2L`

**问题**：注释说"实测 2 轮已经收敛"，但代码中没有收敛诊断（如每轮插补值的变化量）。如果数据或插补逻辑发生变化，2 轮可能不再够用。

---

### 3.5 类别特征编码方式

**位置**：所有 `fit_predict()` 中的 `to_matrix()`

**问题**：3 个类别特征（gender、stress_level、academic_work_impact）被转为整数编码（`as.integer(factor)`），这在 xgboost 中意味着模型会把它们当作有序数值。对于无序类别（如 gender 的 Male/Female/Other），更好的做法是独热编码（one-hot encoding）。不过由于这 3 个特征已被确认是纯噪声，实际影响为零。

---

## 四、架构与设计评价

### 4.1 做得好的地方

| 方面 | 评价 |
|---|---|
| **折内插补防泄漏** | ★★★★★ 这是本项目最正确的设计决策。从模板层强制保证，任何人都不能违反。正是这个设计让后续的"插补越精细越差"结论站得住脚。 |
| **冻结 folds.rds** | ★★★★★ 固定 seed + 保护机制 + 清晰的注释说明为什么不能重跑。所有对比实验可比性的基础。 |
| **文档质量** | ★★★★★ `项目说明.md` 是极好的技术文档——有数据、有推理、有自我推翻、有代码出处。可以直接作为报告的骨架。 |
| **阴性结论文化** | ★★★★★ 项目敢于记录"我们试了，没用"——特征工程无效、缺失指示无效、约束投影只触发 0.06%。这些在学术报告中比正面结果更有价值。 |
| **推理链的严谨性** | ★★★★☆ "假设→证伪→提出新解释→设计检验→再证伪→反向预测→验证"的链条非常漂亮，每一步都有配对检验。 |
| **Tier A/B 分层设计** | ★★★★☆ 20 万对比实验 + 全量集成的两层设计是务实的算力预算方案。 |
| **文件级隔离** | ★★★★☆ 10 人每人一个文件、直推 main 的架构设计适合教学场景。 |

### 4.2 整体架构评分

```
数据工程   ★★★★☆  01_load 的类型统一和完整性检查很扎实
EDA       ★★★★★  8 个发现全部可复现，Simpson 悖论和自推翻是亮点
特征工程   ★★☆☆☆  6 个派生特征全部无效，但 pipeline 设计（分两批）是合理
建模       ★★★☆☆  防泄漏到位，但缺早停、缺调参、缺嵌套验证
评估       ★★★☆☆  配对检验方向正确但样本量太小，缺概率校准
集成       ？？？   未运行，无法评估
可复现     ★★★☆☆  seed 固定做得好，但 renv.lock 缺失是硬伤
```

---

## 五、对各发现的方法论审查

| 发现 | 方法论评价 |
|---|---|
| 发现 1（硬约束 100%） | ✅ 在全量数据上验证，代码可复现。结论正确。 |
| 发现 2（周末冗余） | ✅ 相关 +0.80 和比值 1.33 基于 51.8 万完整行，稳健。 |
| 发现 3（相关结构） | ✅ 使用 pairwise complete obs，合理。 |
| 发现 4（三个噪声特征） | ✅ 控制屏幕时间后比较，方法正确。但消融验证存在选择偏差。 |
| 发现 5（标签近乎确定） | ✅ 分箱单调性清晰。注意：这是 complete-case 上的结论，不代表全量。 |
| 发现 6（MCAR） | ✅ 逐列差值和按缺失个数分组都做了，结论稳健。 |
| 发现 7（Simpson 悖论） | ✅ 边际 vs 条件对比清晰，因果解释合理。 |
| 发现 8（空闲时间） | ✅ 负值比例 1.84% 的量化很关键，避免了错误假设。 |
| 发现 9（通知/打开的重要性） | ✅ 消融实验中唯一有实质效应量的发现。但 p 值基于 n=5 配对 t 检验。 |
| 核心发现（聚 vs 散） | ✅ 解释统一且可证伪。在 glmnet 上的反向预测验证了它。 |

---

## 六、如果我是审稿人，我会问的问题

1. **"你的 xgboost 用了 600 轮，为什么是 600？有没有早停？"** → 1.1
2. **"你做了超参数调优吗？没有调参的情况下，插补策略的排名是否可能反转？"** → 1.2
3. **"5 折的配对 t 检验只有 5 个差值，正态性假设怎么验证？"** → 1.4
4. **"你在全体数据上选了特征，又在同一数据上评估，这不是循环论证吗？"** → 1.5
5. **"L3 的约束到底起了什么作用？如果只触发 0.06%，它和普通的回归插补有什么区别？"** → 2.1
6. **"你证明了派生特征无效，为什么还留着它们？"** → 2.3
7. **"你的模型输出的是概率吗？概率校准了吗？"** → 2.6
8. **"你在 20 万行上得出的结论，在全量 69 万行上还成立吗？"** → 设计文档 3.6

---

## 七、优先级行动清单

如果只能在提交前修 3 件事，修这三件：

| 优先级 | 问题 | 工作量 | 收益 |
|---|---|---|---|
| **P0** | 加早停 + 折内验证集（1.1） | 改模板 + 6 个文件，约 30 分钟 | 消除最大方法论硬伤 |
| **P0** | 做一轮粗粒度超参搜索（1.2） | 只对 L1 xgboost 做，约 1 小时 | 报告中有"调参"可讲 |
| **P1** | 改用 Wilcoxon 检验或报告 effect size（1.4） | 只改报告中的数字，10 分钟 | 避免被统计老师追问 |
| **P1** | 生成 `renv.lock`（3.1） | `renv::snapshot()`，5 分钟 | 满足作业第 2 条要求 |
| **P1** | 将框架区提取为独立文件（2.4） | 30 分钟 | 降低后续修改的出错风险 |
| **P2** | 修复 L4 硬编码 seed（2.2） | 5 分钟 | 消除小隐患 |
| **P2** | 删除无效派生特征或加开关（2.3） | 10 分钟 | 简化 pipeline |
| **P2** | 做概率校准检查（2.6） | 15 分钟 | 报告多一段讨论 |

---

## 八、总结

这是一个**文档驱动、自我批判、推理链清晰**的项目。最令人印象深刻的是它在发现假设被推翻后没有掩饰，而是转向了更深刻的机制解释。这种"我们的假设错了，但我们从错误中学到了更多"的叙事在学术报告中极为有力。

但**方法学上存在两个不容忽视的硬伤**：无早停和无超参调优。这两个问题在机器学习课程项目中是基础要求，缺失它们会让"技术深度"这一项（占 30% 的评分）大打折扣。好消息是这两个问题修复成本不高，且都在交付截止（9/6）前完全来得及。

**最终建议**：保持当前的分析框架和叙事主线不变，集中精力修 P0 和 P1 问题，然后跑 Tier B 全量 + Kaggle 提交。这个项目的方法论底子很好，主要是细节上需要补课。