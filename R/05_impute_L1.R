# =============================================================================
# 05_impute_L1.R —— 第一条线：不插补
#
# 负责人：A
#
# 这条线什么都不做，直接把带 NA 的数据喂给模型。
# 只有 xgboost 和 lightgbm 能这么用 —— 它们在训练时会为每个分裂点
# 学一个「缺失值往左还是往右」的默认方向。glmnet 和 ranger 遇到 NA 会直接报错。
#
# 这是我们的基线：如果后面三条插补线都赢不过它，说明插补这个方向是错的。
#
# 注意 derive_features() 算出来的派生特征也会带 NA（比值的任一分量缺失就是 NA），
# 这是本条线的定义特征，不是 bug。
# =============================================================================

#' 拟合插补器（本条线不需要拟合任何东西）
#'
#' @param dt data.table，训练折的数据
#' @return 空 list，仅为保持接口一致
fit_imputer_L1 <- function(dt) {
  list(line = "L1")
}

#' 应用插补器（本条线原样返回）
#'
#' @param imp fit_imputer_L1 的返回值
#' @param dt data.table，要处理的数据
#' @return 原样的 dt
apply_imputer_L1 <- function(imp, dt) {
  dt
}
