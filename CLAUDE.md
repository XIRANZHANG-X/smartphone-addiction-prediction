# 智能手机成瘾预测 — Agent 工作规则

10 人课程小组项目，纯 R。Kaggle 站内赛 S6E8，指标 ROC-AUC。
面向组员的入口是 [docs/项目总览.md](docs/项目总览.md)，本文件只写**不看到就会犯错的东西**。

## 怎么跑

R 4.6.1 不在 PATH，必须用绝对路径：

```
"D:/R/R-4.6.1/bin/Rscript.exe" R/xx.R
```

数据不在 git 里（68 MB，且竞赛数据不宜公开再分发）。需自行从 Kaggle 下载
`train.csv` / `test.csv` / `sample_submission.csv` 放进 `data/raw/`。

一格实验：写一个 6 行的配置脚本（照抄 `R/06_model_TEMPLATE.R`），
设 `MODEL_NAME` / `TIER` / `IMPUTE_LINE` / `fit_predict`，然后 `source("R/06_framework.R")`。

环境变量覆盖，不必改 14 个模型文件：

| 变量 | 作用 |
|---|---|
| `TIER=B` | 用全量 691,369 行（默认 A，20 万子样本） |
| `REPEAT_ID=1` | 重复 CV，产物进 `output/repeat/` |
| `USE_DERIVED=0` / `USE_TE=0` | 关掉派生特征 / 逐取值编码，用于对照 |
| `POOL_FILE=output/pools/pool_100k.rds` | 用指定的行池代替 TIER 选行，产物进 `output/ladder/` |
| `IMPUTE_CACHE=1` | 缓存并复用每折的插补结果，只对 L4 有意义 |

**`IMPUTE_CACHE` 是给 L4 用的。** L4 的链式随机森林在全量上每格约 134 分钟，
而四个算法跑的是**同一份**插补。缓存后链式随机森林从 40 次降到 10 次，
8.9 小时变 2.6 小时。等价性由 `R/26_cache_check.R` 验证（逐位相同）。
缓存文件在 `output/impute_cache/`，几百 MB，是可重建的中间产物，跑完就该删。

## 四条不可违反的纪律

**1. `output/folds.rds` 冻结后不得重新生成。** 它是全组分数可比的唯一基础，
重跑一次此前所有实验作废。`R/04_folds.R` 有 `I_KNOW_WHAT_IM_DOING` 守卫。

**2. 任何看 y 的变换，只能在折内的训练部分拟合。** 插补器与
target encoder 都是 `fit_* / apply_*` 分离的形状，由 `R/06_framework.R` 在折内调用。
在 CV 循环之外拟合（哪怕用同一套折）就是泄漏——训练部分的每个样本
都会被含验证折标签的统计量编码。

**3. `R/06_framework.R` 是共用框架。** 改行为改这一个文件，14 格同时生效；
不要把早停/防泄漏逻辑复制进模型文件。

**4. 不要往 `output/oof/` 里放实验性产物。** `09_stats.R` 与 `run_tierb.R`
用 `^meta_grid_` 通配，多出来的文件会被当成额外的网格格子。
一次性测量放 `output/feat17/`。

## 度量纪律

- **基线必须先有数字，图才是发现。** 任何"这个特征有用"的说法都要有
  同折配对的 delta、Cohen's d 和逐折同号数；n=5 的 p 值不稳健，不作主要判据。
- **安慰剂列是最便宜的对照**：随机值 + 与真实列相同的缺失模式。实测 −0.00010、
  1/5 同号——任何真实特征都必须比它跑得远。见 `R/18_new_features.R`。
- **不要静默兜底。** `lib_models.R` 里 xgboost 取不到 `best_iteration` 时直接
  `stop()`，不回退到常数——那会把 API 变更伪装成一次正常实验。
- 阴性结论要留证据脚本，不要只留一句话。`R/14`~`R/21` 都是这类存档。
- **「多大算显著」不是数据的性质，是那一对的性质。** `R/23_resolution.R`
  给出配对的分辨率下限：同一个库里，近乎孪生的一对下限 0.000076，
  跨模型族的一对是 0.000680，相差九倍。⚠ ρ 低于约 0.95 时公式会**低估** 15%，
  那时用脚本输出的实测值而非公式值。

## 已定价、但不设为默认的选项

这三项都实测为正、五折同号，但成本与收益不成比例，所以按需启用而非默认：

| 选项 | 增益 | 成本 |
|---|---|---|
| 种子平均（3 个种子秩平均） | +0.00017 | 3× 训练时间 |
| `eta` 0.05 → 0.02 | +0.00013 | 2.2× 训练时间（早停轮数 666 → 1569）|
| `eta` 0.02 → 0.01 | 再 +0.00003 | 再 2× |

分工按讨论区第 46 帖 Tilii 的原话：**搜参与对比实验用 0.05，
最终出交付模型时用 0.02**。测量见 `R/22_cheap_wins.R`。

（对照：**秩空间 logistic 集成 +0.00024 且零成本**，所以它已设为默认，
见 `R/07_ensemble.R` 方法一之二。）

## 已知陷阱（都踩过）

- **插补会破坏逐取值编码。** L3（回归预测）填出的值不在生成器的格点上，
  被插补行中只有 **0.03%~0.19%** 能在训练折查到，编码等于对这些行失效
  （而至少缺一列的行占 61%）。L2 中位数与 L4 的 PMM 填的是格点上的值，
  命中率 100% / 99.98%。**在有编码的流程里，插补必须填回格点上的值** ——
  这与「哪种插补更准」正交。见 docs/实验报告.md 第 10.1 节。

- **★ 测量脚本不要用别名依赖共享函数。** `20_feature_v2.R` 里曾写
  `derive_v1 <- derive_features`（当作「旧口径」），后来更正合并进
  `03_features.R`，`derive_v1` 就跟着变成了新口径——**两个对照臂算的是同一件事，
  差值恰好 0.00000，脚本却仍声称在比较新旧**。不会报错，只会静默地不再测量。
  凡是「A 对 B」的对照，两个口径都要在测量脚本内**显式写死**。
  同类问题此前也出现过（`prepare_fold` 被复制在六处，三处漏跟编码）。

- **★ 差值低于分辨率下限就不是排序。** `10_tune.R` 曾用 5 位小数打印，
  把 L3（0.9522639）和 L4（0.9522585）都显示成 `0.95226`，看上去像同一个数；
  `09_alpha_scan.R` 把相差 0.00001 的四档 alpha 报成「最优 alpha=1」。
  报排序前先用 `R/23_resolution.R` 看这一对的下限。

- **★ 「已存在就跳过」会把陈旧结果当成新结果。** `09_repeated_cv.R`
  默认跳过已有文件，改了特征口径之后重跑，15 个格子全部跳过、
  报的是三周前编码前的数字。换口径后重跑一律带 `FORCE=1`，并先备份旧产物。

- **整数溢出**：`200000L * 98000L` 和 `n1 * n0 ≈ 9.85e10` 都会变 NA。
  行数相乘前一律 `as.numeric()`。
- **`system2(env=)` 在 Windows 上坏掉**（R 会前置 Unix `env` 命令）。
  用 `run_tierb.R` 里的 `run_with_env()`。
- **`sys.source(envir=)` 隔离不了模型文件**（`source()` 在 globalenv 求值）。
  每格用子进程跑。
- **写 R/Python 脚本用 Write 工具，不要用 bash heredoc**，长脚本和转义容易损坏。
- 生成图表时**小差异不要用零基线柱状图**：本项目算法间差 0.03、
  插补策略间只差 0.001~0.01，从 0 起画后者会被压平。

## 当前状态（2026-09-03）

**全部实验已在统一口径（25 特征含编码、冻结折叠）下重跑完毕。**
统一汇总与结论见 **[docs/实验报告.md](docs/实验报告.md)**，那是现在的入口文档。

比赛已于 8/31 结束，最终榜单 0.96627（旧口径九成员集成）。
赛后：**最优单模型 L1_xgboost 0.96784、十四成员集成 0.96807**（无榜单验证）。

**本轮完成的：** 全量 4×4 网格 14 格、Tier A 消融/调参/alpha/校准/交互、
重复 CV（n=15）、讨论区那批 8 个脚本、样本量阶梯（新）、集成权重迁移（新）。

⚠ **口径**：现在只剩**一处**旧口径——`output/oof/meta_grid_*.rds`
（Tier A 编码前的 4×4 网格，即 `results.md` 第一节），保留作历史记录，
备份在 `output/archive_pre_te/`。其余一律是 25 特征含编码。
**新口径的 20 万结果在 `output/ladder/` 下**，不要与 `meta_grid_*` 混读。

**已回答的三个待办：**

① Tier A 带编码重跑 → 由 `R/25_size_ladder.R` 的 200k 那一级顺带完成；
② L4 四格全量 → 完成，缓存后 4.2 小时（不缓存需 15.6 小时）；
③ 「有编码时 L4 应反超 L3」→ **证实**。xgboost 上编码前 L3 0.95951 > L4 0.95791，
   编码后全量 L4 0.95443 > L3 0.94770。glmnet 是唯一例外（L3 仍最好）。

⚠ **本轮新查明的三件事**（细节见实验报告）：
1. 插补会破坏逐取值编码（见下节陷阱）；
2. 子样本选择可迁移——五级阶梯全部 top-1 命中、选择遗憾 0，
   集成权重迁移代价上界 0.00003；但排名错误全部集中在 L3 那几格；
3. 消融结论依赖当时的特征集——加编码后「删掉通知数」的损失缩小 37 倍，
   而「删掉派生特征」从 +0.00012 反转为 −0.00111。

**待办清单**见 [docs/进度.md](docs/进度.md)。
