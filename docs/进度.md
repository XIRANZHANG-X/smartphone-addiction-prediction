# 项目进度

> 最后更新：2026-08-21 · 距交付（9/6）还有 16 天

这份文档是项目状态的唯一真源。**每次有人跑完实验或交付模块，请更新这里。**

---

## 一句话状态

地基全部完成并实测跑通，4×4 实验网格已完成 6/14 格，并且已经得到一个可发表级别的发现。剩下的主要是**铺量**（补齐网格）和**交付物**（报告、幻灯片）。

---

## 已完成 ✅

### 环境与工程（全部实测通过）

| 项 | 状态 | 备注 |
|---|---|---|
| R 4.6.1 环境 | ✅ 实测 | `D:\R\R-4.6.1` |
| 依赖包安装 | ✅ 实测 | xgboost 3.2.1.1 / lightgbm 4.7.0 / ranger 0.18.0 / glmnet 5.0 / missRanger 2.6.1 / data.table 1.18.4 |
| **Rtools** | ✅ **不需要** | 原计划的最大风险点。CRAN 全部提供 R 4.6 预编译包，一次装成 |
| GitHub 仓库 | ✅ | 已推送，凭据走 Git Credential Manager |
| 包库钉 D 盘 | ✅ | `.Renviron` |

### 代码（跑通 = 实际运行成功，不是"写完了"）

| 文件 | 状态 | 说明 |
|---|---|---|
| `R/00_setup.R` | ✅ 跑通 | 装包 + 环境检查 |
| `R/01_load.R` | ✅ 跑通 | 数据读取，9 项完整性断言全过 |
| `R/02_eda.R` | ✅ 跑通 | 8 个发现全部用代码复现 |
| `R/03_features.R` | ✅ 跑通 | `features_raw.rds` 987,671 × 28 |
| `R/04_folds.R` | ✅ 跑通并冻结 | 跨折正例率极差 **0.00000** |
| `R/05_impute_L1.R` | ✅ 跑通 | 不插补 |
| `R/05_impute_L2.R` | ✅ 跑通 | 中位数 / 众数 |
| `R/05_impute_L3.R` | ✅ 跑通 | 约束条件插补 |
| `R/05_impute_L4.R` | ✅ 跑通 | PMM 随机插补 |
| `R/06_model_TEMPLATE.R` | ✅ 跑通 | 防泄漏循环结构已验证 |
| `R/07_ensemble.R` | ⚠️ **写完但从未运行** | 需要 Tier B 产物才能跑 |
| `R/08_submit.R` | ⚠️ **写完但从未运行** | 需要集成结果才能跑 |

### 实验网格 6/14

行 = 插补线，列 = 模型。✅ = 已完成并有 OOF 产物。

| | glmnet | ranger | xgboost | lightgbm |
|---|---|---|---|---|
| **L1** 原生 NaN | —（不支持） | —（不支持） | ✅ **0.95910** | ⬜ |
| **L2** 中位数 | ✅ 0.91775 | ⬜ | ✅ 0.95871 | ⬜ |
| **L3** 约束插补 | ✅ **0.92841** | ⬜ | ✅ 0.95804 | ⬜ |
| **L4** PMM 随机 | ⬜ | ⬜ | ✅ 0.95618 | ⬜ |

均为 Tier A（20 万行子样本）5 折 CV。

---

## 已得到的核心发现

### 关键交互效应

**同一个插补方法，在两类模型上结论完全相反：**

```
xgboost（有原生缺失处理）                glmnet（必须插补）
  L1 原生 NaN     0.95910  ← 最优         L3 约束插补   0.92841  ← 最优
  L2 中位数常数   0.95871                 L2 中位数     0.91775
  L3 条件均值     0.95804
  L4 随机抽真值   0.95618  ← 最差
```

配对检验（同折同行，比看标准差有力得多）：

| 对比 | 差值 | 同号 | t | p |
|---|---|---|---|---|
| L1 − L2 (xgb) | +0.00038 | 4/5 | 3.59 | 0.0231 |
| L2 − L3 (xgb) | +0.00067 | 5/5 | 2.96 | 0.0414 |
| L3 − L4 (xgb) | +0.00186 | 5/5 | 8.49 | 0.0011 |
| L1 − L4 (xgb) | +0.00292 | 5/5 | 26.60 | <0.0001 |
| **L3 − L2 (glmnet)** | **+0.01065** | 5/5 | 69.80 | <0.0001 |

### 机制

**决定因素是插补把缺失行在特征空间里"散开"的程度。**

- L1 把所有未知**聚在一起**，用一个学到的默认分支方向统一处理
- L2 全部填同一个常数，仍然是聚的
- L3 按条件均值散开
- L4 按真实分布随机散开，散得最开

散得越开，需要单独判断的行就越多，出错的机会也越多。glmnet 没有"聚"这个选项，每行都必须用插补值算分，所以猜得准才重要。

> **能把"未知"聚起来处理的模型，聚比猜好；不能聚的模型，猜得准比猜得糙好。**

### 特征消融结论（新增）

| 消融变体 | AUC 变化 | p | 裁决 |
|---|---|---|---|
| 去掉 6 个派生特征 | +0.00011 | 0.112 | 特征工程**完全无效** |
| 去掉 12 个缺失指示 + n_missing | -0.00006 | 0.598 | 确认无用（发现 6 成立） |
| 去掉 gender/stress/impact | +0.00009 | 0.183 | 确认无用（发现 4 成立） |
| 去掉 age | -0.00008 | 0.330 | 确认弱 |
| **去掉 app_opens_per_day** | **-0.00624** | <0.0001 | **原判断错误** |
| **去掉 notifications_per_day** | **-0.00721** | <0.0001 | **原判断错误** |

作为对照：四条插补线之间最大差距（L1 对 L4）只有 0.00292。**这两个被误判为噪声的特征，各自价值是全部插补研究成果的两倍以上。**

机制是**天花板效应**：两者在标签未饱和的低 60% 区间有干净的单调效应（极差 0.04–0.08），在高 40% 区间为零（极差 ≤0.004）；边际分析把两段平均，信号被稀释。且两者方向相反（通知数负向、打开次数正向），进一步互相抵消。详见 [项目说明.md 发现 9](项目说明.md)。

### 三个被证伪的假设（这是好事，不是失败）

1. **"约束条件插补会提升成绩"** —— 在 xgboost 上它是最差的三条之一（p=0.0020）。
2. **"L3 变差是因为回归估计向均值收缩"** —— L4 用 PMM 完全不收缩，结果更差（p=0.0011）。收缩不是主因。
3. **"notifications_per_day 和 app_opens_per_day 是噪声"** —— 消融显示它们是除屏幕时间外最重要的两个特征（p<0.0001）。错在只看边际分布。

三次证伪加一次成功的反向预测（"在不能吃 NaN 的模型上我们的方法应该反赢"，实测赢了一个数量级），这条推理链比"我猜对了"有价值得多，而且每一步都有配对检验撑着。

**第 3 条尤其值得写进报告**：我们在发现 7 里刚写下"单变量分析会骗人"，转头就在发现 4 里用单变量分析把两个关键特征判成了噪声。这个自打脸和发现 7 的 Simpson 悖论互为印证，构成一条主线——*这份数据里任何边际结论都不可信*。

**注意**：约束投影本身实测只在 **0.06%** 的行上触发，L3 目前实质上是链式线性回归插补，"约束"二字名不副实。报告里必须如实说明。

---

## 待完成 ⬜

按优先级排列。

### 高优先级（影响榜单名次）

| 任务 | 负责 | 说明 |
|---|---|---|
| **补齐实验网格剩余 8 格** | 建模组 | lightgbm 全列 + ranger 全列 + L4×glmnet。复制 `06_model_TEMPLATE.R` 改两行配置即可 |
| **超参数调优** | 组长 | 目前全部用的是模板里的默认参数，一轮都没调过。这是当前最大的分数增量来源 |
| **Tier B 全量重训** | 组长 | 选 Tier A 里排名靠前的候选，改 `TIER <- "B"`，产出全量 OOF + test 预测 |
| **跑通 `07_ensemble.R`** | 组长 | 依赖上一项。三种集成方式的对比结果也是报告内容 |
| **跑通 `08_submit.R`** | 组长 | 依赖上一项 |
| **首次 Kaggle 提交** | 组长 | 验证 CV 与 LB 同向。**由组长本人提交** |

### 中优先级（影响评分中的洞察与技术深度）

| 任务 | 负责 | 说明 |
|---|---|---|
| **EDA 可视化** | EDA 组 | `reports/eda.qmd`。优先做发现 **1、5、7、9** 四张图。**发现 9 的"边际平坦 vs 分段单调"对照图是全项目最重要的一张**——它同时展示了我们的错误和修正 |
| **交互效应可视化** | EDA 组 | 上面那张"两类模型方向相反"的对照图，是全项目最值得放大的一张 |
| ~~特征消融实验~~ | ~~分析组~~ | ✅ **已完成**（11 个变体），结论见上方与项目说明发现 9。剩余可做：折内特征选择以消除选择偏差 |
| **特征重要性 / 可解释性** | 分析组 | 看模型认为什么重要，与 EDA 结论是否一致。不一致处特别值得讲 |
| **严格版多重插补** | 建模 L4 | 当前 L4 是单次 PMM。真正的多重插补是 m 份数据各训一个模型、平均**预测**，需要扩展框架区支持一条线出 m 个模型 |

### 交付物（不做就直接扣分）

| 任务 | 负责 | 对应要求 |
|---|---|---|
| **`renv.lock`** | 文档组 | 作业第 2 条明确要求列出安装的 R 包 |
| **复现验收** | 文档组 | 9/5，由未参与编码的人在干净环境跑通全流程 |
| **幻灯片** | 幻灯片组 | 12 分钟 |
| **讲稿与排练** | 协调组 | ≥5 人上台，必须计时排练 |
| **代码注释清理** | 全员 | 作业第 1 条要求"well-commented" |

---

## 已知问题与技术债

| 问题 | 影响 | 处理 |
|---|---|---|
| `07_ensemble.R` / `08_submit.R` 从未运行 | 可能有 bug | 等 Tier B 产物出来后第一时间验证 |
| L4 的 missRanger 很慢 | 单条线约需半小时 | 已把树数降到 50、迭代降到 2。要提高精度需要更好的机器 |
| L3 的"约束"实测只触发 0.06% | 命名与实质不符 | 报告中如实说明；可考虑改进约束设计或直接重命名 |
| 全部结果均基于默认超参数 | 排名不可靠 | 调参后所有结论需复核一遍 |
| **消融的特征筛选存在选择偏差** | CV 分数可能偏乐观 | 判定噪声用了全量训练数据（含标签），严格做法应放进折内。已在项目说明 6.7 声明，需靠 Kaggle 榜独立验证 |
| 6 个派生特征实测无效 | 白做的工作 | 可考虑直接删掉以简化流程；但删不删对分数无影响，优先级低 |

---

## 分工说明

本项目**不使用真实姓名**，全部按角色代号标注，见 [小组分工.md](小组分工.md)。
